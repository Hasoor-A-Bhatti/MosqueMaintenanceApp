<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mosque Profiles</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: url('/Masjid-Mubarak.jpg') no-repeat center center/cover;
            color: white;
            font-family: 'Arial', sans-serif;
        }
        .navbar {
            background: rgba(0, 0, 0, 0.5) !important;
            transition: background 0.3s ease-in-out;
        }
        .navbar:hover {
            background: rgba(0, 0, 0, 0.8) !important;
        }
        .navbar-brand, .nav-link {
            color: white !important;
        }
        .nav-link:hover {
            color: #00ffcc !important;
        }
        .overlay {
            background: rgba(0, 0, 0, 0.6);
            min-height: 100vh;
            padding: 40px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .page-title {
            font-size: 3rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 30px;
            text-shadow: 2px 2px 10px rgba(255, 255, 255, 0.3);
        }
        .mosque-card {
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(15px);
            color: white;
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 25px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            min-height: 300px;
        }
        .mosque-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.35);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.5);
        }
        .card-title {
            font-size: 1.8rem;
            font-weight: bold;
            margin-bottom: 15px;
        }
        .card-text {
            font-size: 1.2rem;
            line-height: 1.5;
        }
        .container {
            max-width: 90%;
        }
        @media (max-width: 768px) {
            .mosque-card {
                padding: 20px;
            }
            .page-title {
                font-size: 2.5rem;
            }
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">Mosque Maintenance</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/mosque_profile">Mosque Profile</a></li>
                    <li class="nav-item"><a class="nav-link" href="/cleaning_sessions">Cleaning Sessions</a></li>
                    <li class="nav-item"><a class="nav-link" href="/maintenance_tasks">Maintenance Tasks</a></li>
                    <li class="nav-item"><a class="nav-link" href="/bookings">Bookings</a></li>
                    <li class="nav-item"><a class="nav-link" href="/administration">Administration</a></li>
                    <li class="nav-item"><a class="nav-link" href="/incidents">Incidents</a></li>
                    <li class="nav-item"><a class="nav-link" href="/contact_us">Contact Us</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <br><br><br><br>

    <div class="overlay">
        <h1 class="page-title">Mosque Profiles</h1>
        <div class="container">
            <div class="row justify-content-center">
                <% if (Mosques && Mosques.length > 0) { %>
                    <% Mosques.forEach(mosque => { %>
                        <div class="col-lg-4 col-md-6 col-sm-12">
                            <div class="mosque-card p-4 shadow">
                                <h3 class="card-title"><%= mosque.name %></h3>
                                <p class="card-text"><strong>Worship Capacity:</strong> <%= mosque.worship_capacity %></p>
                                <p class="card-text"><strong>Car Park Capacity:</strong> <%= mosque.car_park_capacity %></p>
                                <p class="card-text"><strong>Accommodation Capacity:</strong> <%= mosque.accommodation_capacity %></p>
                                <p class="card-text"><strong>Current Accommodation Level:</strong> <%= mosque.current_accommodation_level %></p>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p>No mosque profiles available.</p>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
